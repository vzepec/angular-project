export class Movie {
    title: string;
    year: string;
    img: string;

    constructor(title: string, year: string, img: string) {
        this.title = title;
        this.year = year;
        this.img = img;
    }
}